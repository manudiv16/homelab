apiVersion: v1
kind: Service
metadata:
  name: arroyo
  namespace: monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: arroyo
  ports:
    - port: 5115
      targetPort: 5115
      name: api
