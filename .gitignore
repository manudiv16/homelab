# Secrets and credentials
*.key
*.pem
*.p12
*.pfx
*.env
.env*
kubeconfig*
config.k3s*
*credentials*
*secret*
*.htpasswd

# Kubeconfigs (never commit)
.kube/
kubeconfig

# Local tool state
.atl/
.pi/

# OS files
.DS_Store
Thumbs.db

# Terraform state
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Monitoring secrets — never commit Garage S3 credentials
infrastructure/monitoring/fluentbit/secret.yaml
infrastructure/monitoring/parquet-compactor/secret.yaml

# Tansu secrets — never commit S3 credentials or DSN
infrastructure/tansu/secret.yaml

# IDE
.idea/
.vscode/settings.json
*.swp
*.swo
